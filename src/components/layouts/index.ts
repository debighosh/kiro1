/**
 * Role-specific layout shells (task 1.3).
 *
 * Each layout wraps a family of routes and isolates that role's concerns
 * (design: Frontend Design → role-specific layouts).
 */
export { Public } from './Public';
export { Audience } from './Audience';
export { Admin } from './Admin';
export { Presenter } from './Presenter';
